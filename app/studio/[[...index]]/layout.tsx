export const metadata = {
  title: 'Sanity Studio',
  description: 'Content management for PTE Intensive Blog',
}

export default function StudioLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
